"""Specialist orchestration (Graph, Swarm, A2A).

Implement specialist flows here, for example:

- `from strands.multiagent import GraphBuilder` for deterministic pipelines
- `from strands.multiagent import Swarm` for collaborative teams
- Optional `strands.multiagent.a2a` when agents run as separate services

Keep imports lazy if optional extras are not installed for all environments.
"""


def describe_specialist_plan() -> str:
    """Placeholder until graphs are wired."""
    return (
        "Phase 1 specialists live under l3_runtime/team/ (agents-as-tools). "
        "For gap→liaison→meeting pipelines see docs/NEUUF_ISO_PHASE_PLAN.md (GraphBuilder / Swarm)."
    )
